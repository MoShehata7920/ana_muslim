import 'package:flutter/material.dart';

class DoaaScreen extends StatelessWidget {
  const DoaaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Doaa"));
  }
}
